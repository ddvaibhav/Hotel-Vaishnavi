import { AppRegistry } from 'react-native';
import App from './App';

// Ensure the entry registration matches index.js for reliable native booting
AppRegistry.registerComponent('VaishnaviHotel', () => App);

export default App;
